# bro-cloud (private)

This folder is the expected checkout location for the private bro-cloud Gradle project.

Public contributors only need the obfuscated jar at `libs/bro-cloud-obfuscated.jar`.
Internal developers can replace this folder with the private repo and set `-PbroCloudUseLocal=true`.
