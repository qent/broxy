# MCP Proxy - Мультиплатформенный прокси-сервер для Model Context Protocol

## 📋 Описание проекта

MCP Proxy - это мультиплатформенное приложение на Kotlin, которое выступает в роли интеллектуального прокси-сервера между AI-клиентами (такими как Claude Desktop) и множеством MCP-серверов. Основная идея - создание единой точки входа, которая агрегирует инструменты из различных MCP-серверов и предоставляет их в отфильтрованном виде на основе пресетов (ролей агентов).

### Ключевые возможности

- **🔌 Универсальная совместимость**: Поддержка всех типов транспортов MCP (STDIO, HTTP+SSE, WebSocket)
- **🎯 Интеллектуальная фильтрация**: Создание пресетов с выборочными инструментами для специализированных AI-агентов
- **🖥️ Кроссплатформенность**: Работает на macOS (включая M1) и Windows
- **🎨 Современный UI**: Material 3 Design с темной/светлой темой
- **⚡ CLI режим**: Запуск прокси-сервера из командной строки
- **🔐 Безопасность**: Поддержка переменных окружения для чувствительных данных

## 🏗️ Архитектура приложения

### Концептуальная схема

```
┌─────────────────┐
│  Claude Desktop │
│   (AI Client)   │
└────────┬────────┘
         │ MCP Protocol
         ▼
┌─────────────────┐
│   MCP Proxy     │ ◄── Фильтрация на основе пресета
│    (Server)     │
└────────┬────────┘
         │
    ┌────┴────┬──────────┬──────────┐
    ▼         ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│GitHub  │ │Slack   │ │Database│ │FileSystem│
│Server  │ │Server  │ │Server  │ │Server   │
└────────┘ └────────┘ └────────┘ └────────┘
```

### Модульная структура

1. **core** - Платформенно-независимая бизнес-логика
   - MCP клиент для подключения к downstream серверам
   - MCP сервер для обслуживания upstream клиентов
   - Система фильтрации и маршрутизации
   - Управление конфигурацией

2. **ui** - Desktop приложение на Compose Multiplatform
   - Управление MCP серверами
   - Создание и редактирование пресетов
   - Мониторинг состояния прокси

3. **cli** - Консольное приложение
   - Запуск прокси-сервера
   - Работа с конфигурационными файлами

## 🚀 Как это работает

### 1. Подключение MCP серверов

Пользователь добавляет различные MCP серверы в конфигурацию. Каждый сервер может предоставлять свой набор инструментов (tools), ресурсов (resources) и промптов (prompts).

**Пример конфигурации сервера:**
```json
{
  "mcpServers": {
    "github": {
      "name": "GitHub MCP Server",
      "transport": {
        "type": "stdio",
        "command": "npx",
        "args": ["@modelcontextprotocol/server-github"]
      },
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    },
    "slack": {
      "name": "Slack MCP Server",
      "transport": {
        "type": "http",
        "url": "https://slack-mcp.example.com",
        "headers": {
          "Authorization": "Bearer ${SLACK_TOKEN}"
        }
      }
    }
  }
}
```

### 2. Создание пресетов

Пресет - это набор отфильтрованных инструментов из различных серверов, объединенных для конкретной роли AI-агента.

**Пример пресета "Developer Assistant":**
```json
{
  "id": "developer",
  "name": "Developer Assistant",
  "description": "# Developer Assistant\n\nYou are an experienced software developer...",
  "tools": [
    {
      "serverId": "github",
      "toolName": "create_issue",
      "enabled": true
    },
    {
      "serverId": "github",
      "toolName": "create_pull_request",
      "enabled": true
    },
    {
      "serverId": "filesystem",
      "toolName": "read_file",
      "enabled": true
    },
    {
      "serverId": "filesystem",
      "toolName": "write_file",
      "enabled": true
    }
  ]
}
```

### 3. Запуск прокси-сервера

Прокси-сервер запускается с выбранным пресетом и предоставляет единый MCP endpoint для AI-клиента.

**CLI запуск:**
```bash
java -jar mcp-proxy.jar run \
  --config ~/mcp.json \
  --preset developer \
  --transport stdio
```

**Или через UI:**
1. Открыть приложение MCP Proxy
2. Выбрать пресет
3. Нажать "Start Proxy"
4. Скопировать конфигурацию для Claude Desktop

### 4. Подключение из Claude Desktop

Claude Desktop подключается к прокси-серверу как к обычному MCP серверу:

```json
{
  "mcpServers": {
    "developer-proxy": {
      "command": "java",
      "args": ["-jar", "/path/to/mcp-proxy.jar", "run", "--preset", "developer"]
    }
  }
}
```

## 🎨 Пользовательский интерфейс

### Главное окно приложения

```
┌─────────────────────────────────────────────────┐
│ ☰ MCP Proxy                              🌙 ⚙️ │
├─────────┬───────────────────────────────────────┤
│         │  Servers                               │
│ 📡 Servers │                                     │
│         │  ┌─────────────────────────────┐      │
│ 🎯 Presets │  │ 🟢 GitHub Server          │      │
│         │  │    Type: STDIO             │      │
│ ▶️ Proxy  │  │    Tools: 15              │      │
│         │  │    [Edit] [Test] [Delete]  │      │
│ ⚙️ Settings│  └─────────────────────────────┘      │
│         │                                        │
│         │  ┌─────────────────────────────┐      │
│         │  │ 🔴 Slack Server           │      │
│         │  │    Type: HTTP+SSE          │      │
│         │  │    Tools: 8               │      │
│         │  │    [Edit] [Test] [Delete]  │      │
│         │  └─────────────────────────────┘      │
│         │                                        │
│         │        [+] Add Server                  │
└─────────┴───────────────────────────────────────┘
```

### Редактор пресетов

```
┌─────────────────────────────────────────────────┐
│ Edit Preset: Developer Assistant                 │
├─────────────────────────────────────────────────┤
│ Name: [Developer Assistant                    ] │
│                                                  │
│ Description (Markdown):                         │
│ ┌──────────────────────────────────────────┐   │
│ │ # Developer Assistant                     │   │
│ │                                           │   │
│ │ You are an experienced software developer │   │
│ │ specialized in...                         │   │
│ └──────────────────────────────────────────┘   │
│                                                  │
│ Tools Selection:                   🔍 [Search] │
│ ┌──────────────────────────────────────────┐   │
│ │ ▼ 📁 GitHub Server (3/15 selected)       │   │
│ │   ☑ create_issue                         │   │
│ │   ☑ create_pull_request                  │   │
│ │   ☐ list_issues                          │   │
│ │   ☑ search_code                          │   │
│ │   ☐ get_file_contents                    │   │
│ │                                           │   │
│ │ ▶ 📁 Slack Server (0/8 selected)         │   │
│ │                                           │   │
│ │ ▼ 📁 FileSystem Server (2/5 selected)    │   │
│ │   ☑ read_file                            │   │
│ │   ☑ write_file                           │   │
│ │   ☐ list_directory                       │   │
│ └──────────────────────────────────────────┘   │
│                                                  │
│ Selected: 5 tools from 3 servers                │
│                                                  │
│        [Cancel]            [Save Preset]        │
└─────────────────────────────────────────────────┘
```

## 🔧 Технические детали

### Поддерживаемые транспорты

1. **STDIO** (Standard Input/Output)
   - Для локальных процессов
   - Используется Claude Desktop по умолчанию
   - Пример: Node.js серверы, Python скрипты

2. **HTTP+SSE** (Server-Sent Events)
   - Для удаленных серверов
   - REST API для запросов, SSE для событий
   - Пример: Облачные MCP серверы

3. **WebSocket**
   - Двунаправленная связь в реальном времени
   - Низкая латентность
   - Пример: Real-time серверы

### Фильтрация и маршрутизация

#### Префиксирование имен

Для избежания конфликтов имен, инструменты префиксируются именем сервера:

- Оригинал: `create_issue` (от GitHub сервера)
- После префикса: `github.create_issue`

#### Маршрутизация запросов

Когда клиент вызывает `github.create_issue`, прокси:
1. Извлекает префикс `github`
2. Находит соответствующий сервер
3. Удаляет префикс из имени инструмента
4. Отправляет запрос `create_issue` на GitHub сервер
5. Возвращает ответ клиенту

### Управление жизненным циклом

1. **Запуск прокси:**
   - Загрузка конфигурации
   - Подключение к downstream серверам
   - Получение capabilities от каждого сервера
   - Применение фильтров пресета
   - Запуск upstream сервера для клиентов

2. **Обработка запросов:**
   - Прием запроса от клиента
   - Определение целевого сервера
   - Проксирование запроса
   - Возврат ответа клиенту

3. **Graceful shutdown:**
   - Завершение активных запросов
   - Отключение от downstream серверов
   - Закрытие upstream сервера
   - Сохранение состояния

## 📦 Установка и запуск

### Системные требования

- **Java**: 11 или выше
- **ОС**: macOS (включая Apple Silicon M1), Windows 10/11
- **Память**: Минимум 512MB RAM
- **Дисковое пространство**: 100MB

### Desktop приложение

1. Скачать дистрибутив для вашей ОС:
   - macOS: `MCP-Proxy-1.0.0.dmg`
   - Windows: `MCP-Proxy-1.0.0.msi`

2. Установить приложение

3. Запустить MCP Proxy

4. Добавить MCP серверы в настройках

5. Создать пресеты для различных ролей

### CLI режим

1. Скачать `mcp-proxy.jar`

2. Создать конфигурационные файлы:
   - `~/mcp.json` - конфигурация серверов
   - `~/preset_developer.json` - пресет для разработчика

3. Запустить прокси:
```bash
java -jar mcp-proxy.jar run \
  --config ~/mcp.json \
  --preset ~/preset_developer.json \
  --transport stdio
```

### Интеграция с Claude Desktop

1. Запустить прокси-сервер (UI или CLI)

2. Добавить в конфигурацию Claude Desktop:
```json
{
  "mcpServers": {
    "mcp-proxy": {
      "command": "java",
      "args": [
        "-jar",
        "/path/to/mcp-proxy.jar",
        "run",
        "--config", "~/mcp.json",
        "--preset", "developer",
        "--transport", "stdio"
      ]
    }
  }
}
```

3. Перезапустить Claude Desktop

## 🔐 Безопасность

### Хранение чувствительных данных

- **Переменные окружения**: Токены и ключи API хранятся в переменных окружения
- **Конфигурационные файлы**: Поддержка `${VAR_NAME}` синтаксиса
- **Безопасное логирование**: Токены маскируются в логах

### Примеры использования переменных окружения

```json
{
  "env": {
    "GITHUB_TOKEN": "${GITHUB_TOKEN}",
    "OPENAI_API_KEY": "${OPENAI_API_KEY}",
    "DATABASE_URL": "${DATABASE_URL}"
  }
}
```

Установка переменных:
```bash
export GITHUB_TOKEN="ghp_xxxxxxxxxxxx"
export OPENAI_API_KEY="sk-xxxxxxxxxxxx"
```

## 🧪 Тестирование

### Тестирование подключения к серверу

1. В UI: Нажать кнопку "Test Connection" рядом с сервером
2. В CLI: 
```bash
java -jar mcp-proxy.jar test-server --config ~/mcp.json --server github
```

### Проверка пресета

```bash
java -jar mcp-proxy.jar validate-preset --preset ~/preset_developer.json
```

### Отладочный режим

```bash
java -jar mcp-proxy.jar run \
  --config ~/mcp.json \
  --preset developer \
  --log-level DEBUG
```

## 📚 Примеры пресетов

### Developer Assistant
Инструменты для разработки ПО:
- GitHub: управление issues и PR
- FileSystem: чтение/запись файлов
- Terminal: выполнение команд
- Database: запросы к БД

### Data Analyst
Инструменты для анализа данных:
- Database: SQL запросы
- FileSystem: работа с CSV/JSON
- Python: выполнение скриптов
- Visualization: создание графиков

### Content Writer
Инструменты для написания контента:
- FileSystem: работа с документами
- Grammar: проверка грамматики
- Research: поиск информации
- Publishing: публикация контента

### DevOps Engineer
Инструменты для DevOps:
- Kubernetes: управление кластером
- Docker: работа с контейнерами
- AWS: управление облачными ресурсами
- Monitoring: метрики и алерты

## 🛠️ Разработка

### Сборка из исходников

```bash
# Клонировать репозиторий
git clone https://github.com/your-org/mcp-proxy.git
cd mcp-proxy

# Сборка всех модулей
./gradlew build

# Запуск тестов
./gradlew test

# Сборка desktop приложения
./gradlew :ui:packageDistributionForCurrentOS

# Сборка CLI JAR
./gradlew :cli:shadowJar
```

### Структура проекта

```
mcp-proxy/
├── core/           # Бизнес-логика
├── ui/             # Desktop UI
├── cli/            # CLI приложение
├── docs/           # Документация
├── samples/        # Примеры конфигураций
└── tests/          # Интеграционные тесты
```

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! См. [CONTRIBUTING.md](CONTRIBUTING.md) для деталей.

## 📄 Лицензия

MIT License - см. [LICENSE](LICENSE) для деталей.

## 🔗 Полезные ссылки

- [Model Context Protocol Specification](https://modelcontextprotocol.io)
- [MCP Kotlin SDK](https://github.com/modelcontextprotocol/kotlin-sdk)
- [Compose Multiplatform](https://www.jetbrains.com/lp/compose-multiplatform/)
- [Примеры конфигураций](./samples/)

## ❓ FAQ

**Q: Можно ли использовать MCP Proxy с другими AI клиентами, кроме Claude Desktop?**
A: Да, любой клиент, поддерживающий MCP протокол, может подключиться к прокси-серверу.

**Q: Сколько серверов можно подключить одновременно?**
A: Ограничений нет, но рекомендуется не более 10-15 для оптимальной производительности.

**Q: Поддерживается ли hot-reload конфигурации?**
A: В UI есть кнопка "Reload Configuration". В CLI режиме требуется перезапуск.

**Q: Можно ли создать пресет, который включает ВСЕ инструменты?**
A: Да, можно создать пресет без фильтрации, который проксирует все доступные инструменты.

**Q: Как обновить приложение?**
A: Desktop версия поддерживает автообновление. Для CLI скачайте новую версию JAR файла.

## 📞 Поддержка

- **Issues**: [GitHub Issues](https://github.com/your-org/mcp-proxy/issues)
- **Discussions**: [GitHub Discussions](https://github.com/your-org/mcp-proxy/discussions)
- **Email**: support@mcp-proxy.example.com